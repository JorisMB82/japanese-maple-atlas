# Editorial inbox

This directory contains governed contributor inputs. Files in `contributors/` and `submissions/` are source inputs to the Atlas Compiler and must pass `npm run validate:contributions`. Generated repository objects must never be edited in place.

Use `npm run contribution:new -- --target RC-001 --type evidence-addition --title "Title" --contributor CTR-CONTRIBUTOR-ID` to scaffold a draft.
