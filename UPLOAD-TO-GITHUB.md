# Upload Release 0.2

Upload everything inside this folder to the repository root. GitHub should replace files with matching names and add the new files and folders.

Commit message:

`Release 0.2: ship usable Atlas product interface`

Do not upload the ZIP itself or nest this folder inside the repository.
